version: '3'
services:
  zookeeper:
    image: zookeeper
    restart: always
    ports:
      - "2181:2181"
  scratchpad-java:
    image: anapsix/alpine-java
    volumes:
      - ./playground:/playground:ro
